package com.example.checkersguifinal;

public class GameLogic {
    public static void main(String[] args){
        /*
        CheckersAI fred= new CheckersAI(Board.Player.RED);
        while (black.gameState() == Board.State.ONGOING) {
            if(black.getCurrentPlayer() == Board.Player.BLACK) {
                TreeMap<IntegerMove> result = todd.getBestMove(black);
                Move move = result.get(result.firstKey());
                black.move(move.getPiece(), move.getDirection());
                red.move(move.getPiece(), move.getDirection());
            }else{
                TreeMap<Integer, Move> result = fred.getBestMove(red);
                Move move = result.get(result.firstKey());
                black.move(move.getPiece(), move.getDirection());
                red.move(move.getPiece(), move.getDirection());
            }
        }
        double endTime = (double) (System.currentTimeMillis() - startTime) / 1000;
        System.out.println(black);
        System.out.println(black.getNumMoves() + " moves");
        System.out.println(endTime + " seconds");
        System.out.println(endTime / black.getNumMoves() + " seconds per move");

         */


    }



}
